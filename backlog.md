# Глобальный бэклог — отложенные задачи на уровне ~/.claude/

## quick-learning + skill-trainer — править вместе, после остальных скиллов

Это единственные два скилла, которые **пишут** в методологию, а не исполняют её:
`quick-learning` извлекает уроки сессии в `reasoning-patterns.md` / `triad-index.md`,
`skill-trainer` вшивает накопленные триады в тела остальных скиллов.

Поэтому их нельзя править первыми. Пока формат остальных скиллов не устоялся под
Opus 5, тренер будет вшивать правила старого образца, и работу придётся переделывать.
Порядок: сначала привести к новому виду исполняющие скиллы, потом — эти два.

Что в них конкретно пересматривать под Opus 5:

- **Сигнальный гейт `quick-learning`** построен на признаках, которые давала прошлая
  модель: число `fix:` коммитов, раунды ревью, повторные чтения файлов. На Opus 5
  раундов меньше по другой причине — модель чинит себя сама, — и гейт будет молчать
  на сессиях, где урок был.
- **Формат триады** проектировался под скиллы с чеклистами самопроверки. После
  расщепления чеклистов на «машинные / рассуждение / не проверка» поле `Adapted`
  указывает на конструкции, которых в скиллах больше нет.
- **`skill-trainer` вшивает правила текстом в тело SKILL.md.** Часть уроков теперь
  корректнее вшивать не текстом, а машинной проверкой в блок «Проверки фактом» —
  иначе тренер восстанавливает ровно те леса, которые из скиллов убирали.
- **Оба спавнят агентов** (`skill-trainer` — по одному на скилл параллельно). Нужен
  явный критерий делегирования: Opus 5 делегирует охотнее, чем нужно.

Опыт для проверки — тот же, что для остальной правки: одна и та же сессия прогоняется
на `master` и на новой ветке, меряется не только расход токенов, но и число извлечённых
уроков. Упало число уроков — гейт зарезали слишком глубоко.
